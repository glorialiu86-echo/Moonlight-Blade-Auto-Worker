# 项目记录
状态：执行到一半

## 仓库位置

- GitHub 仓库：`https://github.com/glorialiu86-echo/Moonlight-Blade-Auto-Worker`
- 当前工作目录：`C:\Users\ZMT-User\Downloads\天刀控制系统`

## 当前机器结论

- 当前仓库这轮代码改动发生在 macOS
- 最终真机运行目标仍是 Windows 主机
- 游戏安装目录：`D:\天涯明月刀`
- 当前已确认游戏窗口标题：`天涯明月刀手游`

## 当前代码真实进度

已完成：

- 本地 `Node` 服务、`.env` 加载、千问文本链路
- 截图感知主链路已切到本地 `RapidOCR` OCR worker
- 文本规划链路已支持单独切到本地 `Ollama`
- Windows 本地 Python 环境重建，`faster-whisper` 与输入执行器可用
- 自动截图采集链路接入主服务
- 固定游戏窗口截图可直接运行
- 真实 Windows 输入执行器已接入主链路
- `/api/chat` 可触发 `规划 -> Windows 输入执行 -> 状态回填`
- 自动截图可持续刷新 `latestPerception`
- 自动截图调度已按目标频率补齐剩余时间
- 路人 NPC 对话链路已明确切到点击 owner，`F` 只保留给功能型 NPC
- 本机已拉起 `qwen2.5:3b`，文本规划可走本地 `Ollama`
- NPC 交互样本已开始自动落盘，可生成本地学习总结
- 路人 NPC 交互已从“点击后模糊判定成功”改为固定点击链：
  - 点击 NPC
  - 点击 `查看`
  - 点击右下角 `交谈`
  - 点击 `闲聊`
  - 若出现 `闲聊确认` 则点击确认，否则直接进入聊天界面
- 前端已拆成主播首页 `/` 和调试页 `/debug`
- 主播首页已加入明确的开始/停止任务按钮
- 自动化主流程已切到固定剧本 runner：
  - 正常买货叫卖 `3` 轮
  - 正常社交套话 `2` 轮
  - 黑化社交套话 `2` 轮
  - 潜行、闷棍、偷窃 `1` 轮
- `/api/chat` 已改成“布置整套任务并等待启动”，不再当场让 planner 决定自主主流程
- 前端消息流已改为只展示人格化思考和执行结果，不显示阶段、轮次或倒计时

仍未完成：

- 动作语义仍是最小映射，当前主要落到 `inspect/focus`、`esc`、`wait`
- 还没有把“闲聊确认后真正进入可输入状态”的检测做稳定
- 还没有把送礼补好感、买礼物、送礼重试这一整条链路做完
- 用户抢占、执行中断、恢复策略仍是骨架
- 自动运行策略还没有针对真控风险做完整约束

## 本轮新增结论

- 固定剧本自动化现在是唯一主流程真源，LLM 只负责前端思考外观与局部聊天内容
- 路人 NPC 的唯一稳定交互真源已收敛为阶段化点击链
- 后端只有在真正进入聊天界面后，才允许继续尝试自动发闲聊回复
- 已新增 `docs/specs/town-npc-gift-and-chat-flow.md`，把聊天入口、交易拿礼物、赠礼提好感三条链路合并成统一真源
- 已新增 `docs/specs/fixed-script-automation-flow.md`，明确固定剧本自动化与前端展示边界
- 已确认 NPC 分类规则：蓝名普通 NPC 走 `10` 点阈值，蓝名下带白色 `<>` 称号的特殊 NPC 走 `50` 点阈值
- `talk` 动作已切到单一社交闭环 owner：优先尝试聊天，失败后自动执行 `交易 -> 赠礼 -> 再试聊天`
- 已补出可单独验证的底层原子动作：`短距离前进`、`右键拖拽转视角`、`相对坐标点击`
- `走动` / `转视角` 的主判定已收敛为本地规则视觉：先采样待机噪声基线，再用场景差分做 `pass/review/fail`
- `review` 边界样本已自动落盘到 `data/motion-review/`，并由本地文本模型异步做低频复核；模型不参与主控制判定
- 路人 NPC 交互已开始改成快速突击链：优先点右侧街道人群，命中目标后立即推进 `查看 -> 交谈 -> 闲聊`，不再在前半段频繁 OCR 和乱动镜头

## 当前主要风险

- 现在已经能更稳定地点进路人 NPC 交互，但“确认后是否进入可输入聊天框”仍依赖后续界面识别
- 送礼链路尚未打通，因此低好感 NPC 的完整闭环还没完成
- 真实输入已经接入，后续所有主动动作都需要继续收紧安全边界
- 当前本地模型复核仍基于规则指标文本而不是视觉图像语义，本轮先完成“边界样本留痕 + 低频复核器”闭环
